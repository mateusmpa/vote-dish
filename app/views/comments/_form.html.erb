<%= form_for @comment, html: { class: "form-horizontal" } do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :content, class: "col-sm-2 control-label" %><br>
    <%= f.text_area :content, class: "form-control", rows: "3" %>
  </div>
  <div class="form-group">
    <%= f.label :commentable_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :commentable_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :commentable_type, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :commentable_type, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
