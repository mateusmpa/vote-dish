<%= form_for @restaurant, html: { class: "form-horizontal", multipart: true } do |f| %>
  <% if @restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% @restaurant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.file_field :avatar %>

  <div class="form-group">
    <%= f.label :name, class: "col-md-2 control-label" %>
    <div class="col-md-10">
      <%= f.text_field :name, class: "form-control", placeholder: :name %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :address, class: "col-md-2 control-label" %>
    <div class="col-md-10">
      <%= f.text_field :address, class: "form-control", placeholder: :address %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :specialty, class: "col-md-2 control-label" %>
    <div class="col-md-10">
      <%= f.text_field :specialty, class: "form-control", placeholder: :speciality %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-md-offset-2 col-md-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
